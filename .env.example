# ============================================================
# FinanceFlow — Environment Variables
# ============================================================
# Copy this file to `.env` and fill in the values.
#
# NOTE: The Firebase client-side config is currently hardcoded
# in src/config/firebase.js. If you migrate to env-based config,
# prefix variables with VITE_ so Vite exposes them to the client.
# ============================================================

# Firebase Client Configuration (optional — currently hardcoded)
# VITE_FIREBASE_API_KEY=your_api_key
# VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your_project_id
# VITE_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
# VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
# VITE_FIREBASE_APP_ID=your_app_id

# Firebase Admin SDK (for scripts in /scripts)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/serviceAccountKey.json
